# inspired by https://securitylab.github.com/research/github-actions-preventing-pwn-requests/
name: Deploy ArazzoUI

on:
  workflow_run:
    workflows: ["Jentic Arazzo Tools Build"]
    types:
      - completed
    branches:
      - main

jobs:

  deploy:
    if: >
      ${{ github.event.workflow_run.event == 'push' &&
      github.ref == 'refs/heads/main' &&
      github.event.workflow_run.conclusion == 'success' }}
    name: Deploy ArazzoUI
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6
      - name: 'Download arazzo-ui-build artifact'
        uses: actions/github-script@v8
        with:
          script: |
            const allArtifacts = await github.rest.actions.listWorkflowRunArtifacts({
               owner: context.repo.owner,
               repo: context.repo.repo,
               run_id: context.payload.workflow_run.id,
            });
            const matchArtifact = allArtifacts.data.artifacts.filter((artifact) => {
              return artifact.name == "arazzo-ui-build"
            })[0];
            const download = await github.rest.actions.downloadArtifact({
               owner: context.repo.owner,
               repo: context.repo.repo,
               artifact_id: matchArtifact.id,
               archive_format: 'zip',
            });
            const fs = require('fs');
            fs.writeFileSync('${{github.workspace}}/arazzo-ui-build.zip', Buffer.from(download.data));
      - run: |
          mkdir deploy-dir
          unzip arazzo-ui-build.zip -d deploy-dir
      - name: Inject Google Tag Manager
        if: ${{ env.GTAG_ID != '' }}
        env:
          GTAG_ID: ${{ secrets.GTAG_ID }}
        run: |
          GTAG_SNIPPET=$(envsubst '$GTAG_ID' < packages/jentic-arazzo-ui/config/gtag.html | tr '\n' ' ')
          sed -i "s|<!-- ANALYTICS -->|${GTAG_SNIPPET}|" deploy-dir/index.html
          grep -q "googletagmanager.com" deploy-dir/index.html || { echo "::error::GTM injection failed — ANALYTICS placeholder not found in index.html"; exit 1; }
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4.8.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: gh-pages # The branch the action should deploy to.
          folder: deploy-dir # The folder the action should deploy.
          clean: true # Automatically remove deleted files from the deploy branch
